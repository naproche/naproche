\documentclass{stex}
\usepackage{naproche}
\begin{document}
\begin{smodule}{subclasses-of-sets-are-sets.ftl}
  \importmodule[naproche/examples/preliminaries]{axioms?elements-of-classes-are-objects.ftl}

  \begin{flemma*}
    Let $X$ be a set and $Y$ be a class.
    Assume that every element of $Y$ is an element of $X$.
    Then $Y$ is a set.
  \end{flemma*}
  \begin{fproof}
    Define $Z = \fclass{x \in X}{\text{$x$ is an element of $Y$}}$.
    Then $Z$ is a set and $Z = Y$.
  \end{fproof}
\end{smodule}
\end{document}
