module SAD.Core.Cache where

-- import Data.Map (Map)
-- import qualified Data.Map as Map
-- import Data.Text (Text)

-- import SAD.Core.Transform

-- TODO: remember to invoke e with --proof-object
-- we should check for equal proofs and all hypothesis being present

-- data CacheEntry = CacheEntry
  -- { proof :: [Prf]
  -- , hypothesis :: [Term] -- ^ the hypothesis that were actually used
  -- } deriving (Eq, Ord, Show, Read)

-- data Cache = Map Term CacheEntry
  -- deriving (Eq, Ord, Show, Read)

-- readCache :: FilePath -> Cache
-- readCache = undefined