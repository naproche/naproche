\documentclass{stex}
\libusepackage{naproche}
\libinput{preamble}
\begin{document}
\begin{smodule}{length-of-reversion.ftl}

\importmodule[libraries/lists]{theorems?length-of-concatenation.ftl}
\importmodule[libraries/lists]{definitions?reversion.ftl}

\begin{proposition}[forthel,id=LengthOfRevProp]
  $\length(\rev(L)) \eq \length(L)$ for any list $L$.
\end{proposition}
\begin{proof}[forthel,method=induction on $L$]
  Let $L$ be a list.
  
  \begin{case}{$L \eq \list{}$.} \end{case}
  
  \begin{case}{$L \eq a \cons L'$ for some object $a$ and some list $L'$.}
    Take an object $a$ and a list $L'$ such that $L \eq a \cons L'$.
    Then $L' \prec L$.
    Hence $\length(\rev(L')) \eq \length(L')$.
    Thus
    \begin{align*}
      \length(\rev(L))
        &\eq \length(\rev(a \cons L'))\\
        &\eq \length(\rev(L') \concat \list{a})\\
        &\eq \length(\list{a}) \plus \length(\rev(L'))\\
        &\eq \one \plus \length(\rev(L'))\\
        &\eq \one \plus \length(L')\\
        &\eq \length(L)
    \end{align*}.
  \end{case}
\end{proof}

\end{smodule}
\end{document}
