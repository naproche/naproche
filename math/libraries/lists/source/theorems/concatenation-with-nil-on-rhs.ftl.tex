\documentclass{naproche-library}
\libinput{preamble}
\begin{document}
\begin{smodule}[title=Concatenation with Nil on the Right-Hand Side]{concatenation-with-nil-on-rhs.ftl}

\importmodule[libraries/lists]{definitions?concatenation.ftl}

\begin{proposition}[forthel,id=RightConcatNilProp]
  $L \concat \list{} \eq L$ for every list $L$.
\end{proposition}
\begin{proof}[forthel,method=induction on $L$]
  Let $L$ be a list.

  \begin{case}{$L \eq \list{}$.} \end{case}
  
  \begin{case}{$L \eq a \cons L'$ for some object $a$ and some list $L'$.}
    Consider an object $a$ and a list $L'$ such that $L \eq a \cons L'$.
    Then $L' \prec L$.
    Hence $L' \concat \list{} \eq L'$.
    Thus
    \begin{align*}
      L \concat \list{}
        &\eq (a \cons L') \concat \list{}\\
        &\eq a \cons (L' \concat \list{})\\
        &\eq a \cons L'\\
        &\eq L
    \end{align*}.
  \end{case}
\end{proof}

\end{smodule}
\end{document}
