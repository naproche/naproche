\documentclass{naproche-library}
\libinput{preamble}
\begin{document}
\begin{smodule}[title=Reversion is an Involution]{reversion-is-involution.ftl}

\importmodule[libraries/lists]{theorems?reversion-of-concatenation.ftl}

\begin{proposition}[forthel,id=RevIsInvolutionProp]
  $\rev(\rev(L)) \eq L$ for every list $L$.
\end{proposition}
\begin{proof}[forthel,method=induction on $L$]
  Let $L$ be a list.

  \begin{case}{$L \eq \list{}$.} \end{case}

  \begin{case}{$L \eq a \cons L'$ for some object $a$ and some list $L'$.}
    Take an object $a$ and a list $L'$ such that $L \eq a \cons L'$.
    Then $L' \prec L$.
    Hence $\rev(\rev(L')) \eq L'$.
    Thus
    \begin{align*}
      \rev(\rev(L))
        &\eq \rev(\rev(a \cons L'))\\
        &\eq \rev(\rev(L') \snoc a)\\
        &\eq a \cons \rev(\rev(L'))\\
        &\eq a \cons L'\\
        &\eq L
    \end{align*}.
  \end{case}
\end{proof}

\end{smodule}
\end{document}
