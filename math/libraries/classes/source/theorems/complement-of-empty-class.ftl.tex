\documentclass{naproche-library}
\libinput{preamble}
\begin{document}
\begin{smodule}[title=Complement of the Empty Class]{complement-of-empty-class.ftl}

\importmodule[libraries/classes]{definitions?difference.ftl}
\importmodule[libraries/classes]{definitions?empty-class.ftl}

\begin{proposition}[forthel,id=DifferenceWithEmptyClassProp]
  Let $A$ be a class.
  Then $A \diff \emptyclass \eq A$.
\end{proposition}
\begin{proof}[forthel]
  $A \diff \emptyclass = \{ x \mid x \elem A\text{ and }x \nelem \emptyclass \}$.
  No element is an element of $\emptyclass$.
  Hence $A \diff \emptyclass = \{ x \mid x \elem A \}$.
  Then we have the thesis.
\end{proof}

\end{smodule}
\end{document}