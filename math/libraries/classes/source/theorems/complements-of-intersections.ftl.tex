\documentclass{naproche-library}
\libinput{preamble}
\begin{document}
\begin{smodule}[title=Complements of Intersections]{complements-of-intersections.ftl}

\importmodule[libraries/classes]{definitions?difference.ftl}
\importmodule[libraries/classes]{definitions?intersection.ftl}
\importmodule[libraries/classes]{definitions?union.ftl}
\importmodule[libraries/classes]{definitions?subclasses.ftl}

\begin{proposition}[forthel,id=DifferenceWithIntersectProp]
  Let $A, B, C$ be classes.
  Then $A \diff (B \intersect C) \eq (A \diff B) \union (A \diff C)$.
\end{proposition}
\begin{proof}[forthel]
  Let us show that $A \diff (B \intersect C) \subclass (A \diff B) \union (A \diff C)$.
    Let $x \elem A \diff (B \intersect C)$.
    Then $x \elem A$ and $x \nelem B \intersect C$.
    Hence it is wrong that ($x \elem B$ and $x \elem C$).
    Thus $x \nelem B$ or $x \nelem C$.
    Therefore $x \elem A$ and ($x \nelem B$ or $x \nelem C$).
    Then ($x \elem A$ and $x \nelem B$) or ($x \elem A$ and $x \nelem C$).
    Hence $x \elem A \diff B$ or $x \elem A \diff C$.
    Thus $x \elem (A \diff B) \union (A \diff C)$.
  End.

  Let us show that $((A \diff B) \union (A \diff C)) \subclass A \diff (B \intersect C)$. %!
    Let $x \elem (A \diff B) \union (A \diff C)$.
    Then $x \elem A \diff B$ or $x \elem A \diff C$.
    Hence ($x \elem A$ and $x \nelem B$) or ($x \elem A$ and $x \nelem C$).
    Thus $x \elem A$ and ($x \nelem B$ or $x \nelem C$).
    Therefore $x \elem A$ and not ($x \elem B$ and $x \elem C$).
    Then $x \elem A$ and not $x \elem B \intersect C$.
    Hence $x \elem A \diff (B \intersect C)$.
  End.
\end{proof}

\end{smodule}
\end{document}