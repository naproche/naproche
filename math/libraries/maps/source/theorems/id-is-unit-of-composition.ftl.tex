\documentclass{stex}
\libusepackage{naproche}
\libinput{preamble}
\begin{document}
\begin{smodule}{id-is-unit-of-composition.ftl}

\importmodule[libraries/maps]{definitions?maps-from-classes-to-classes.ftl}
\importmodule[libraries/maps]{definitions?id.ftl}
\importmodule[libraries/maps]{definitions?comp.ftl}

\begin{proposition}[forthel,id=IdIsUnitOfCompProp]
  Let $A, B$ be classes and $f \fromto{A}{B}$.
  Then $f \compo \id{A} \eq f \eq \id{B} \compo f$.
\end{proposition}
\begin{proof}[forthel]
  $A$ is the domain of $f \compo \id{A}$ and the domain of $f$ and the domain of $\id{B} \compo f$.
  We have $(f \compo \id{A})(a)
    \eq f(\id{A}(a))
    \eq f(a)
    \eq \id{B}(f(a))
    \eq (\id{B} \compo f)(a)$
  for all $a \elem A$.
  Hence $f \compo \id{A}
    \eq f
    \eq \id{B} \compo f$.
\end{proof}

\end{smodule}
\end{document}