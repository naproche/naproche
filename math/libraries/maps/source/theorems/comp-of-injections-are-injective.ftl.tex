\documentclass{stex}
\libusepackage{naproche}
\libinput{preamble}
\begin{document}
\begin{smodule}{comp-of-injections-are-injective.ftl}

\importmodule[libraries/maps]{definitions?injections.ftl}
\importmodule[libraries/maps]{definitions?comp.ftl}
\importmodule[libraries/maps]{definitions?maps-of-classes.ftl}

\begin{proposition}[forthel,id=CompOfInjectMapsIsInjectProp]
  Let $A, B, C$ be classes and $f \frominto{A}{B}$ and $g \frominto{B}{C}$.
  Then $g \compo f$ is an injective map from $A$ to $C$.
\end{proposition}
\begin{proof}[forthel]
  $g \compo f$ is a map of $A$.

  Let us show that $g \compo f$ is injective.
    Let $a, a' \elem A$.
    Assume $(g \compo f)(a) \eq (g \compo f)(a')$.
    Then $g(f(a)) \eq g(f(a'))$.
    Hence $f(a) \eq f(a')$.
    Indeed $f(a), f(a') \elem B$.
    Thus $a \eq a'$.
  End.
\end{proof}

\end{smodule}
\end{document}