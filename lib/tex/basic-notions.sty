\NeedsTeXFormat{LaTeX2e}

\RequirePackage{pgffor}
\RequirePackage{amssymb}
\RequirePackage{amsmath}


% Operations and relations
% ------------------------

\let\succ\relax
\let\exp\relax

\DeclareMathOperator{\succ}{succ}
\DeclareMathOperator{\pred}{pred}
\DeclareMathOperator{\id}{id}
\DeclareMathOperator{\range}{range}
\DeclareMathOperator{\add}{add}
\DeclareMathOperator{\mul}{mul}
\DeclareMathOperator{\exp}{exp}
\DeclareMathOperator{\fac}{fac}

\newcommand{\symdiff}{\mathop{\triangle}}
\newcommand{\pow}{\mathcal{P}}
\renewcommand{\div}{\mathop{\operatorname{div}}}
\renewcommand{\mod}{\mathop{\operatorname{mod}}}
\newcommand{\onto}{\twoheadrightarrow}
\newcommand{\into}{\hookrightarrow}
\newcommand{\Ord}{\mathrm{Ord}}
\newcommand{\Field}{\mathrm{Field}}

\newcommand{\const}[2]{\mbox{const}_{#1,#2}}


% Enumerative set notation

% E.g. `\set{1, 2, 3, 4}` prints the numbers 1, 2, 3 and 4 as a comma-separated
% list which is enclosed within curly braces.
\def\set#1{%
  \ensuremath{%
    \{%
    \foreach[count=\i] \x in {#1}{%
      \ifnum\i>1,\,\fi%
      \x%
    }%
    \}
  }%
}
